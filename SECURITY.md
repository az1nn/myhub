# Security

Security design is not complete yet.

Canonical next artifact: `docs/security/threat-model.md`.

Never introduce covert tracking. Never log secrets, authentication tokens,
invitation tokens, precise locations or message contents by default.
